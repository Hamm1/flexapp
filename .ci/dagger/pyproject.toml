[project]
name = "ci"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = ["dagger-io"]

[tool.uv.sources]
dagger-io = { path = "sdk", editable = true }

[build-system]
requires = ["hatchling==1.29.0", "protobuf==7.34.0"]
build-backend = "hatchling.build"
